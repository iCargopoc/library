import {
    Meta,
    Story,
    Props,
    Preview,
    ArgsTable
} from "@storybook/addon-docs/blocks";
import { WithKnobs, text, array, object } from "@storybook/addon-knobs";
import Spreadsheet from "../src/index";
import GridComponent from "./index.js";

<Meta title="Spreadsheet Component" component={Spreadsheet} />

# Spreadsheet

## Business component for showing list of data in Spreadsheet

`Spreadsheet` component consists of list of data. Inline edit of cell will be enabled by double clicking the cell's.

### Props

<Props of={Spreadsheet} />

### Code

```jsx
import { Spreadsheet } from "@neo-ui/spreadsheet";

<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    gridHeight={gridHeight}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isGlobalSearch={isGlobalSearch}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
/>;
```

### Variants

#### With record count

<Story name="With record count">
    <GridComponent
        isTitle={true}
        isGlobalSearch={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
        isAddRow={false}
        isDeleteRow={false}
    />
</Story>

```jsx
<Spreadsheet
    isTitle={isTitle}
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isGlobalSearch={isGlobalSearch}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    isAddRow={isAddRow}
    isDeleteRow={isDeleteRow}
/>
```

#### With custom height

<Story name="With custom height">
    <GridComponent gridHeight={500} isSelectAll={false} />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    gridHeight={gridHeight}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isSelectAll={isSelectAll}
/>
```

#### With global search

<Story name="With global search">
    <GridComponent
        isTitle={false}
        isGlobalSearch={true}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
        isAddRow={false}
        isDeleteRow={false}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    isAddRow={isAddRow}
    isDeleteRow={isDeleteRow}
/>
```

#### With group sort

<Story name="With group sort">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isGroupSort={true}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
        isAddRow={false}
        isDeleteRow={false}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    isAddRow={isAddRow}
    isDeleteRow={isDeleteRow}
/>
```

#### With column chooser

<Story name="With column chooser">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isGroupSort={false}
        isColumnChooser={true}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
        isAddRow={false}
        isDeleteRow={false}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    isAddRow={isAddRow}
    isDeleteRow={isDeleteRow}
/>
```

#### With export data

<Story name="With export data">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={true}
        isSelectAll={false}
        isHeader={true}
        isAddRow={false}
        isDeleteRow={false}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    isAddRow={isAddRow}
    isDeleteRow={isDeleteRow}
/>
```

#### With select all

<Story name="With select all">
    <GridComponent isSelectAll={true} />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isSelectAll={isSelectAll}
/>
```

#### Without header

<Story name="Without header">
    <GridComponent isSelectAll={false} isHeader={false} />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
/>
```

#### With classname

<Story name="With classname">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={false}
        noBorderClassName={"grid-no-border"}
        isAddRow={false}
        isDeleteRow={false}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    noBorderClassName={noBorderClassName}
    isAddRow={isAddRow}
    isDeleteRow={isDeleteRow}
/>
```

#### With add row

<Story name="With add row">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
        isAddRow={true}
        isDeleteRow={false}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    isAddRow={isAddRow}
    isDeleteRow={isDeleteRow}
/>
```

#### With delete row

<Story name="With delete row">
    <GridComponent
        isTitle={false}
        isGlobalSearch={false}
        isGroupSort={false}
        isColumnChooser={false}
        isExportData={false}
        isSelectAll={false}
        isHeader={true}
        isAddRow={false}
        isDeleteRow={true}
    />
</Story>

```jsx
<Spreadsheet
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isTitle={isTitle}
    isGlobalSearch={isGlobalSearch}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    isAddRow={isAddRow}
    isDeleteRow={isDeleteRow}
/>
```

#### With all props

<Story name="With all props">
    <GridComponent
        isTitle={true}
        isTitle={true}
        gridHeight={460}
        isGlobalSearch={true}
        columnFilterStyle={"txt-wrap"}
        isGroupSort={true}
        isColumnChooser={true}
        isExportData={true}
        isSelectAll={true}
        isHeader={true}
        noBorderClassName={"grid-no-border"}
        isAddRow={true}
        isDeleteRow={true}
    />
</Story>

```jsx
<Spreadsheet
    isTitle={isTitle}
    rows={data.slice(0, pageSize)}
    dataSet={data}
    pageSize={pageSize}
    count={pageSize}
    columns={columns}
    gridHeight={gridHeight}
    updateCellData={updateCellData}
    selectBulkData={selectBulkData}
    maxLeftPinnedColumn={maxLeftPinnedColumn}
    updatedRows={updatedRows}
    isGlobalSearch={isGlobalSearch}
    columnFilterStyle={"columnFilterStyle"}
    isGroupSort={isGroupSort}
    isColumnChooser={isColumnChooser}
    isExportData={isExportData}
    isSelectAll={isSelectAll}
    isHeader={isHeader}
    noBorderClassName={noBorderClassName}
    isAddRow={isAddRow}
    isDeleteRow={isDeleteRow}
/>
```
