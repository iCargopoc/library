import {
    Meta,
    Story,
    Props,
    Preview,
    ArgsTable
} from "@storybook/addon-docs/blocks";
import { withKnobs, text, array, object } from "@storybook/addon-knobs";
import Grid from "../src/index";
import GridComponent from "./index.js";
import CustomPanel from "./panels/CustomPanel";

<Meta title="Grid Component" component={Grid} />

# Grid

## Business component for showing a large list of data in the Grid view

### Grid properties

```jsx

// Normal Grid
const gridData = [ // Should be Array of Objects, where each object represents each row.
        {...}, // Each row data should contain a unique property (like PrimaryKey) - idAttribute
        {...}
    ];

const idAttribute = "travelId"; // String - node value of primary key value from gridData

const columns = [
        {
            groupHeader: "Group Header", // String, Optional - If different columns have to be grouped under a single column heading.
            Header:() => <div> Header </div>, // String or JSX - used to display in the gid column heading and other overlays.
            title: "Header", // String, Optional - This has to be given if Header is returning JSX, to display in overlays.
            accessor: "flight", // String - corresponding to the node name in the grid data.
            isSortable: true, // Boolean, Optional - If this column has to be included in the group sort options (Sort By).
            innerCells: [ // If the value of accessor node is object or array (multiple data to be displayed in a single column)
                {
                    Header: "Cell Header", // String - Cell header to be displayed in overlays
                    accessor: "flightno", // String - Child node of accessor mentioned above ("flight")
                    isSortable: true // Boolean, Optional - If this cell has to be included in the group sort options (Sort On).
                },
                {...}
            ],
            width: 50, // Number - For single-line Grid, pass width % allocated for each column, for multi line pass pixel value.
            widthGrow: 1, // Number, Optional - This is the factor that Grid uses to identify how to allocate the width of a hidden column to other columns.
            display: false, // Boolean, Optional - To hide a column by default. The default value for this property is true.
            pinLeft: true, // Boolean, Optional - To keep a column pinned to left by default.
            disableSortBy: true, // Boolean, Optional - To disable column sorting for a column.
            sortValue: "flightno", // Boolean, Optional - Use only if inner cells are present, to decide on which cell column sort should happen.
            disableResizing: true, // Boolean, Optional - To disable column resizing by dragging.
            searchKeys: [  // Array<String>, Optional - Mention all node paths from Grid data, that have to be included in column filtering and global filtering.
                "flight.flightno", // For Object structure - flight: {flightno: "value"}
                "flight.connectionflights.*.airlinename"  // For Array structure - flight: {connectionflights: [{airlinename: "value"}]}
            ],
            displayCell: (rowData, DisplayTag, isDesktop, isColumnExpanded) => { // JSx - content to be displayed in a column.
                // rowData -> Single row data from gridData.
                // DisplayTag -> HOC provided by Grid, to control the show/hide of data inside each cell.
                // isDesktop -> Whether Grid considers this screen as desktop or tablet.
                // isColumnExpanded -> If the row of this data is in expanded state or not.
                const { travelId } = rowData;
                return (
                    <div> {travelId} </div>
                );
            },
            editCell: (rowData, DisplayTag, rowUpdateCallBack, isDesktop, isColumnExpanded) => { // JSx, Optional - content to be displayed in edit overlay of a column.
                // Other parameters are the same as mentioned in displayCell
                // rowUpdateCallBack -> This is a function that has to be called with updated rowData, each time when the user makes any changes to the content.
            },
            exportData: (rowData, isDesktop) => { // Array<Object>, Optional - content to be exported from column.
                // parameters are same as mentioned above.
                // This should list of columns to be displayed in the PDF/Excel/CSV export file.
                return [
                    { // Each object corresponds to each column.
                        header: "Flight No", // String - Heading of each export column.
                        content: {flightno} // String - Value of a row corresponding to each column.
                    },
                    {...}
                ];
                /** NOTE: The number of objects in the returned array should be the same for all rows. Else, the report will go wrong.   **/
            }
        },
        {...}
    ]

const columnToExpand = {
        Header: "Remarks",
        innerCells: [
            {
                Header: "Remarks",
                accessor: "remarks",
                searchKeys: ["remarks"]
            }
        ],
        displayCell: (rowData, DisplayTag, isDesktop) => {
            const { remarks } = rowData;
            return (
                <div>{remarks}</div>
            );
        },
        exportData: (rowData, isDesktop) => { // Array<Object>, Optional - content to be exported from column.
            // parameters are same as mentioned above.
            // This should list of columns to be displayed in the PDF/Excel/CSV export file.
            const { remarks } = rowData;
            return [
                { // Each object corresponds to each column.
                    header: "Remarks", // String - Heading of each export column.
                    content: {remarks} // String - Value of a row corresponding to each column.
                },
                {...}
            ];
            /** NOTE: The number of objects in the returned array should be the same for all rows. Else, the report will go wrong.   **/
        }
    };

// Tree Grid
const treeGridData = [ // Should be Array of Objects, where each object represents each parent row, and children of each parent row should come under the property childData
        {
            ... // Each parent row data should contain a unique property (like PrimaryKey) - parentIdAttribute
            childData:{
                lastPage: false, // Page Info details of each parent row should be present in childData
                pageNum: 1,
                pageSize: 50,
                data: [{...}, {...}] // Should be same as gridData mentioned above
            }
        },
        {...}
    ];

const parentIdAttribute = "titleId"; // String - node value of primary key value from treeGridData

const parentColumn = {
        Header: "Parent Header", // String
        innerCells: [ // If value of accessor node is object or array (multiple data to be displayed in single column)
                {
                    Header: "Cell Header", // String - Cell header to be displayed in overlays
                    accessor: "flightno" // String - Child node of accessor mentioned above ("flight")
                },
                {...}
            ],
        displayCell: (rowData, isExpanded) => { // JSx - content to be displayed in a column.
            // rowData -> Single row data from gridData.
            // isExpanded -> If the parent row is in expanded state or not.
            return (
                <div> Parent Header </div>
            );
        },
        exportData: (rowData, isDesktop) => { // Array<Object>, Optional - content to be exported from column.
            // parameters are same as mentioned above.
            // This should list of columns to be displayed in the PDF/Excel/CSV export file.
            return [
                { // Each object corresponds to each column.
                    header: "Flight No", // String - Heading of each export column.
                    content: {flightno} // String - Value of a row corresponding to each column.
                },
                {...}
            ];
            /** NOTE: The number of objects in the returned array should be the same for all rows. Else, the report will go wrong.   **/
        }
    };

// Sub Component Grid
const subComponentGridData = [
        {
            ..., // Normal JSON data same as gridData mentioned above
            subComponentData: [ // This property holds the row data to be displayed in the sub component section.
                    {...}, // Similar to gridData, where each sub component row data should contain a unique property (like PrimaryKey) - subComponentIdAttribute
                ]
        },
        {...}
    ];

const subComponentIdAttribute = "hawbId";  // String - node value of primary key value from subComponentGridData

const subComponentColumns = [
        {
            Header:() => <div> Header </div>, // String or JSX - used to display in the gid column heading and other overlays.
            title: "Header", // String, Optional - This has to be given if Header is returning JSX, to display in overlays.
            accessor: "flight", // String - corresponding to the node name in the grid data.
            isSortable: true, // Boolean, Optional - If this column has to be included in the group sort options (Sort By).
            innerCells: [ // If the value of accessor node is object or array (multiple data to be displayed in a single column)
                {
                    Header: "Cell Header", // String - Cell header to be displayed in overlays
                    accessor: "flightno", // String - Child node of accessor mentioned above ("flight")
                    isSortable: true // Boolean, Optional - If this cell has to be included in the group sort options (Sort On).
                },
                {...}
            ],
            width: 50, // Number - For single-line Grid, pass width % allocated for each column, for multi line pass pixel value.
            widthGrow: 1, // Number, Optional - This is the factor that Grid uses to identify how to allocate the width of a hidden column to other columns.
            display: false, // Boolean, Optional - To hide a column by default. The default value for this property is true.
            pinLeft: true, // Boolean, Optional - To keep a column pinned to left by default.
            searchKeys: [  // Array<String>, Optional - Mention all node paths from Grid data, that have to be included in column filtering and global filtering.
                "flight.flightno", // For Object structure - flight: {flightno: "value"}
                "flight.connectionflights.*.airlinename"  // For Array structure - flight: {connectionflights: [{airlinename: "value"}]}
            ],
            displayCell: (rowData, DisplayTag, isDesktop, isColumnExpanded) => { // JSx - content to be displayed in a column.
                // rowData -> Single row data from gridData.
                // DisplayTag -> HOC provided by Grid, to control the show/hide of data inside each cell.
                // isDesktop -> Whether Grid considers this screen as desktop or tablet.
                // isColumnExpanded -> If the row of this data is in expanded state or not.
                const { travelId } = rowData;
                return (
                    <div> {travelId} </div>
                );
            },
            editCell: (rowData, DisplayTag, rowUpdateCallBack, isDesktop, isColumnExpanded) => { // JSx, Optional - content to be displayed in edit overlay of a column.
                // Other parameters are the same as mentioned in displayCell
                // rowUpdateCallBack -> This is a function that has to be called with updated rowData, each time when the user makes any changes to the content.
            },
            exportData: (rowData, isDesktop) => { // Array<Object>, Optional - content to be exported from column.
                // parameters are same as mentioned above.
                // This should list of columns to be displayed in the PDF/Excel/CSV export file.
                return [
                    { // Each object corresponds to each column.
                        header: "Flight No", // String - Heading of each export column.
                        content: {flightno} // String - Value of a row corresponding to each column.
                    },
                    {...}
                ];
                /** NOTE: The number of objects in the returned array should be the same for all rows. Else, the report will go wrong.   **/
            }
        },
        {...}
    ]

const subComponentColumnToExpand // Same as columnToExpand

// By default Grid will consider paginationType value as "index".
    const paginationType = "index"; // or - paginationType = "cursor".

    // If paginationType = "index"
    const pageInfo = {
        pageNum: 1,
        pageSize: 300,
        total: 20000,
        lastPage: false // or - true
    };
    // Else if paginationType = "cursor"
    const pageInfo = {
        endCursor: 299,
        pageSize: 300,
        total: 20000,
        lastPage: false // or - true
    };

    const getRowInfo = (rowData, isSubComponentRow) => {
        // Grid calls this function for each row, to check:
        // 1) If expand icon is required for that row.
        // 2) If row selector checkbox is required for that row.
        // 3) If any className has to be given for that row data container div.
        // First parameter is rowData for which this function will be called.
        // Second parameter is a boolean value that identifies if the row is sub component row or parent row.
        const { travelId } = rowData;
        return {
            isRowExpandable: travelId % 2 === 0, // Pass true/false based on which expand icon will be displayed.
            isRowSelectable: travelId % 3 !== 0, // Pass true/false based on which row selector checkbox will be displayed.
            className: travelId % 10 === 0 ? "disabled" : "" // Pass className to be added.
        };
    };

    // Call back made by Grid for each row, to display the row actions in Kebab overlay in Grid.
    const rowActions = (rowData, closeOverlay, isSubComponentRow) => {
        // Grid will call this function for all rows while binding row data into the Grid.
        // Corresponding 'rowData' is passed as one of the parameters.
        // Second parameter is the 'closeOverlay', which is a method that can be called to close the Kebab overlay.
        // Third parameter is a boolean value that identifies if the row is sub component row or parent row.
        // Return a JSX from this function, which will be bound to the row actions Kebab overlay.
        // All functionalities of the row actions should be handled outside the Grid.
        return <RowAction />
    };

    const onRowUpdate = (originalRow, updatedRow, isSubComponentRow) => {
        // Grid will call this function whenever user edits a cell or a row.
        // Row data before editing and the updated row data after editing will be passed as parameters.
        // Third parameter is a boolean value that identifies if the editted row is sub component row or parent row.
        // Using these data, update the data source and rerender the Grid.
    };

    const onRowSelect = (selectedRows, deSelectedRows) => {
        // Grid will call this function when user selects or deselects a row.
        // An array of all selected row(s) data will be passed in as the first parameter.
        // An array of deselected row will be passed in as the second parameter.
        /************
            Make sure:
            * If a row is selected, remove that row details from the 'rowsToDeselect' prop value (if present).
            * If a row is deselected, remove that row details from the 'rowsToSelect' prop value (if present).
        ************/
    };

    const onSearch = (filteredRows) => {
        // Grid will call this function when user applies column/global filters.
        // An array of filtered row(s) data will be passed.
    };

    const onSubComponentRowSelect = (selectedRows, deSelectedRows) => {
        // Grid will call this function when user selects or deselects a row from sub component section.
        // An array of all selected row(s) data will be passed in as the first parameter.
        // An array of deSelectedRow will be passed in as the second parameter.
    };

    // This should be an array of idAttribute values, of rows that have to be deselected from the Grid.
    const rowsToDeselect = [];

    // This should be an array of idAttribute values, of rows that have to be selected from the Grid.
    const rowsToSelect = [];

    const onGridRefresh = () => {
        // Grid will show a refresh icon in the Grid header region if this function is passed as prop to the Grid.
        // Grid will call this function when user cliks that icon.
    };

    const loadMoreData = (updatedPageInfo, parentId, isReload) => {
        /* Grid without tree view */

        // Grid will call this function only if pageInfo is passed a prop to Grid and the value of lastPage in the pageInfo is false.
        // This function is called when user reaches bottom row of the Grid by scrolling down.
        // Grid passes in 3 parameters updatedPageInfo, parentId and isReload.
        // parentId and isReload will be null/undefined in case of Grid without tree view.
        // updatedPageInfo will be:
        // 1) If paginationType is "index", updatedPageInfo will be { (pageNum + 1), pageSize}
        // 2) Else if paginationType is "cursor", updatedPageInfo will be { endCursor, pageSize}
        // Based on the updatedPageInfo, make API call and re-render the Grid with updated data.

        /* Grid with tree view */

        // Grid will call this function when user clicks the parent row expand icon for the forst time or the load more button present at the end of child data.
        // Grid passes in 3 parameters updatedPageInfo, parentId and isReload.
        // parentId will be the parentIdAttribute value of the selected parent.
        // isReload will be true, if this function is called as part of data reload scenario (During child data loading, if value of total property gets changed loadMoreData for that parent will be called again with parameter isReload as true)
        // updatedPageInfo will be null if isReload is true or if child data of that parent is getting called for the first time (clicks the parent row expand icon for the forst time).
        // If user is clicking the load more button present at the end of child data, updatedPageInfo will be:
        // 1) If paginationType is "index" for the corresponding parent, updatedPageInfo will be { (pageNum + 1), pageSize}
        // 2) Else if paginationType is "cursor" for the corresponding parent, updatedPageInfo will be { endCursor, pageSize}
        // Based on the updatedPageInfo and parentId, make API call and re-render the Grid with updated data.
    };

    // True if more column data has to be shown while clicking the row expand icon
    // By default Grid considers expandableColumn as false
    const expandableColumn = true;

    // This should be a React component, which will be rendered into the Grid header.
    const CustomPanel = <CustomPanel />

    // False if the Grid needs only single row selection
    // By default Grid considers multiRowSelection as true
    const multiRowSelection = false;

    // Number of rows to be overscanned by react-window
    // If prop is not passed, Grid takes default value as 5.
    const rowsToOverscan = 4;

    // False if Grid column headings have to be hidden
    // If row selectors are enabled, a checkbox to select all rows will appear at the header portion (before the Grid title)
    // By default Grid considers gridHeader as true
    const gridHeader = true;

    // False if Grid column headings in sub component section has to be hidden
    // By default Grid considers subComponentHeader as true
    const subComponentHeader = true;

    // False if Grid column title has to be hidden
    // By default Grid considers showTitle as true
    const showTitle = true

    // False if row selector column has to be removed from Grid
    // By default Grid considers rowSelector as true
    const rowSelector = true;

    // False if globalSearch functionality is not needed
    // By default Grid considers globalSearch as true
    const globalSearch = true;

    // False if column filter functionality is not needed
    // By default Grid considers columnFilter as true
    const columnFilter = true;

    // False if column group sort functionality is not needed
    // By default Grid considers groupSort as true
    const groupSort = true;

    // False if column column chooser functionality is not needed
    // By default Grid considers columnChooser as true
    const columnChooser = true;

    // False if export data functionality is not needed
    // By default Grid considers exportData as true
    const exportData = true;

    // If the file name of exported files from Grid has to be customized, use this prop.
    // Pass a string value and in that case, this will be the file name of exported files (with different extensions).
    // If prop is not specified Grid uses 'iCargo Neo Report' as the file name.
    const fileName = "Custom filename";

    // This should be a string value. If prop is specified this will be the paper size for PDF export.
    // By default the paper size for PDF export will be A4.
    const pdfPaperSize = "A1"  // Use A1, A2, A3, A4 or A5 - Default value is A4

    // If the theme is 'portal', Grid will assign a different style for the Grid header portion and Grid rows.
    // All other themes will have default stylings.
    const theme = "portal"

    // If all rows of Grid require the same height, Grid will only measure the height of the first row.
    // And then uses this row height for all other rows.
    const fixedRowHeight = true;

    // Pass this function as a prop to Grid, then Grid will callback this function when user applies group sorting.
    const serverSideSorting = (groupSortOptions) => {
        // Sort gridData prop value based on the sort parameters passed in.
        // This function should re-render the Grid with sorted gridData prop.
        // groupSortOptions is the Object representation of the sorting options that user has selected.
        // Example:
        [
            {
                "sortBy": "segment", // This will be the accessor value of the column
                "sortOn": "from", // This will be the accessor value of the innerCell (or just string "value" if no innerCells are present)
                "order": "Ascending" // Either "Ascending" or "Descending"
            },
            {
                "sortBy": "flight",
                "sortOn": "flightno",
                "order": "Descending"
            },
            {
                "sortBy": "travelId",
                "sortOn": "value",
                "order": "Ascending"
            }
        ]
    }

    // Pass this function as a prop to Grid, then Grid will callback this function when user tries to export data.
    // This function will be called to fetch grid data of each page.
    // Return an object containing data and pageInfo.
    const serverSideExporting = (updatedPageInfo) => {
        // updatedPageInfo - pageInfo based on which developer has to fetch ad return gridData.
        // Initially updatedPageInfo will be undefined. Return first page data and the pageInfo in that case.
        // Pass an object like:
        return {
            { data: <searchedData>, pageInfo: <modifiedPageInfo> }
        }
        // This function will be called recursively until the developer passes the pageInfo object that has 'lastPage: true' in it.
        // Data passed each time will be concatenated and this data will be exported from Grid.

        /*********************
        NOTE: In the case of the tree view grid, this function will be called only once. Grid expects complete data to be returned in that single call.
        return {
            { data: <completeData> }
        }
        *********************/
    }

    // Pass this property if a 'loading graphics' has to be displayed in the Grid.
    // This can be used while doing functionalities like server side sorting.
    const displayLoader = true;

    // Pass this property if the pin column feature has to be enabled for grid columns.
    // User can 'Pin Left' columns from manage column overlay, and row actions column will be pinned right by default.
    const enablePinColumn = true;

    // Pass this property if rows in Grid have to be pinned to the top.
    // This should be an array of idAttribute values, of rows that have to be pinned to the top.
    // The UI part for implementing 'Pin Row' functionality (Functionality to be used by the user) has not been implemented yet. Waiting to get the design from the Ux team.
    // Also, the pinned row identifier icon in UI has not been decided yet. This is also pending.
    const rowsToPin = [6];

    // Pass this property if you are aware of an estimated row height value or if you are using the Grid with fixed row height.
    const estimatedRowHeight = 50;

    <Grid
        className="icargoCustomClass"
        theme={theme}
        title="AWBs"
        gridWidth="100%"
        gridData={gridData}
        estimatedRowHeight={estimatedRowHeight}
        rowsToOverscan={rowsToOverscan}
        idAttribute={idAttribute}
        paginationType={paginationType}
        pageInfo={pageInfo}
        loadMoreData={loadMoreData}
        serverSideSorting={serverSideSorting}
        displayLoader={displayLoader}
        columns={columns} // *Required
        columnToExpand={columnToExpand}
        parentColumn={parentColumn} // Only for tree view
        parentIdAttribute={parentIdAttribute} // Only for tree view
        parentRowExpandable={parentRowExpandable} // Only for tree view
        parentRowsToExpand={parentRowsToExpand} // Only for tree view
        subComponentIdAttribute={subComponentIdAttribute}
        subComponentColumns={subComponentColumns} // Only for Sub component grid
        subComponentColumnToExpand={subComponentColumnToExpand} // Only for Sub component grid
        subComponentHeader={subComponentHeader} // Only for Sub component grid
        onSubComponentRowSelect={onSubComponentRowSelect}
        rowActions={rowActions}
        expandableColumn={expandableColumn}
        onRowUpdate={onRowUpdate}
        onRowSelect={onRowSelect}
        onSearch={onSearch}
        getRowInfo={getRowInfo}
        onGridRefresh={onGridRefresh}
        CustomPanel={CustomPanel}
        rowsToSelect={rowsToSelect}
        rowsToDeselect={rowsToDeselect}
        fixedRowHeight={fixedRowHeight}
        multiRowSelection={multiRowSelection}
        gridHeader={gridHeader}
        showTitle={showTitle}
        rowSelector={rowSelector}
        globalSearch={globalSearch}
        columnFilter={columnFilter}
        groupSort={groupSort}
        columnChooser={columnChooser}
        exportData={exportData}
        serverSideExporting={serverSideExporting}
        fileName={fileName}
        pdfPaperSize={pdfPaperSize}
        enablePinColumn={enablePinColumn}
        rowsToPin={rowsToPin}
    />
```

### Variants

#### With custom classname

<Story name="With custom classname">
    <GridComponent className="icargoCustomClass" />;
</Story>

```jsx
// Class name 'icargoCustomClass' will be given for the most outer div in Grid.

<Grid
    className="icargoCustomClass"
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With custom theme

<Story name="With custom theme">
    <GridComponent passTheme />;
</Story>

```jsx
// Grid will be loaded with design matching to the 'portal' theme.

<Grid
    theme="portal"
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With custom title

<Story name="With custom title">
    <GridComponent title="AWBs" />;
</Story>

```jsx
// Gid title will be '<no: rows> AWBs'.

<Grid
    title="AWBs"
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With custom grid width

<Story name="With custom grid width">
    <GridComponent gridWidth="75%" />;
</Story>

```jsx
// Grid width will be '75%' of the container.

<Grid
    gridWidth="75%"
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With estimated row height value

<Story name="With estimated row height value">
    <GridComponent passEstimatedRowHeight />;
</Story>

```jsx
// Value passed in 'estimatedRowHeight' property will be used as default row heigh at the time of rendering and then Grid will calculate the height of each row.
// This can be used to ensure a smooth transition while scrolling down the rows.

<Grid
    title="AWBs"
    gridData={gridData}
    columns={columns}
    estimatedRowHeight={estimatedRowHeight}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With fixed row size

<Story name="With fixed row size">
    <GridComponent fixedRowHeight passEstimatedRowHeight />;
</Story>

```jsx
// All rows will have the same height (height of the first row).
// Only the height of the first row will be calculated and this height will be used for all other rows.
const fixedRowHeight = true;

<Grid
    title="AWBs"
    gridData={gridData}
    columns={columns}
    fixedRowHeight={fixedRowHeight}
    estimatedRowHeight={estimatedRowHeight}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>;
```

#### With row expansion

<Story name="With row expansion">
    <GridComponent passColumnToExpand />;
</Story>

```jsx
// Refer columnToExpand in the 'Grid properties' section (top).
// Row expand icon will be displayed in a separate column in Grid (Last column).

<Grid
    gridData={gridData}
    columns={columns}
    columnToExpand={columnToExpand}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With column expansion

<Story name="With column expansion">
    <GridComponent expandableColumn />;
</Story>

```jsx
// expandableColumn = true - isColumnExpanded value in display cell function parameters to show more data while expanding a row.
// expandableColumn = false - isColumnExpanded value in display cell function parameters will be null/undefined. Use this to show complete data at once.

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    expandableColumn
/>
```

#### With row actions

<Story name="With row actions">
    <GridComponent passRowActions />;
</Story>

```jsx
// Refer rowActions in the 'Grid properties' section (top).
// Passed JSX element will be displayed inside the kebab menu against each row.

<Grid
    gridData={gridData}
    columns={columns}
    rowActions={rowActions}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With pin column

<Story name="With pin column">
    <GridComponent passRowActions isHorizontalGrid passPinColumn />;
</Story>

```jsx
// Refer columns in the 'Grid properties' section (top).
// To keep columns pinned to left, by default, use pinLeft=true while configuring the columns.
// enablePinColumn = true - if the option for user to pin a column manually, has to be enabled.

<Grid
    gridData={gridData}
    columns={columns}
    rowActions={rowActions}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    enablePinColumn={true}
/>
```

#### With pin row

<Story name="With pin row">
    <GridComponent passIdAttribute passPinRow pinnedRowsList={[6]} />;
</Story>

```jsx
// enablePinRow = true - if the option for user to pin a row manually, has to be enabled.
// Pass idAttribute values of rows that are supposed to be pinned by default, in an array.
const rowsToPin = [6];

<Grid
    gridData={gridData}
    columns={columns}
    idAttribute={idAttribute}
    rowActions={rowActions}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    rowsToPin={rowsToPin}
    enablePinRow={true}
/>;
```

#### With grid refresh

<Story name="With grid refresh">
    <GridComponent passOnGridRefresh />;
</Story>

```jsx
// Refer onGridRefresh in the 'Grid properties' section (top).

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    onGridRefresh={onGridRefresh}
/>
```

#### With custom header panel

<Story name="With custom header panel">
    <GridComponent CustomPanel={CustomPanel} />;
</Story>

```jsx
// This should be a React component, which will be rendered into the Grid header.
const CustomPanel = <CustomPanel />

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    CustomPanel={CustomPanel}
/>
```

#### With single row selection

<Story name="With single row selection">
    <GridComponent multiRowSelection={false} passIdAttribute />;
</Story>

```jsx
// This should be a boolean value. Value 'Flase' will disable multi selection and enable single selection and true will do vice-versa
const multiRowSelection = false;

<Grid
    gridData={gridData}
    idAttribute={idAttribute}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    multiRowSelection={multiRowSelection}
/>;
```

#### With rows to be selected

<Story name="With rows to be selected">
    <GridComponent passIdAttribute rowsForSelection={[1, 7, 17]} />;
</Story>

```jsx
// Refer onRowSelect in the 'Grid properties' section (top).
// This should be an array of idAttribute values of rows that has to be selected by default from the Grid.
const rowsToSelect = [1, 7, 17];

<Grid
    gridData={gridData}
    idAttribute={idAttribute}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    rowsToSelect={rowsToSelect}
/>;
```

#### With rows to be deselected

<Story name="With rows to be deselected">
    <GridComponent passIdAttribute />;
</Story>

```jsx
// Refer onRowSelect in the 'Grid properties' section (top).
// This should be an array of idAttribute values of rows that has to be deselected from the Grid.
const rowsToDeselect = []; // [1] or [1, 7]

<Grid
    gridData={gridData}
    idAttribute={idAttribute}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    rowsToDeselect={rowsToDeselect}
/>;
```

#### With pagination

<Story name="With pagination">
    <GridComponent hasPagination />;
</Story>

```jsx
// Refer paginationType, pageInfo and loadMoreData in the 'Grid properties' section (top).
// By default the Grid will consider paginationType value as "index".

<Grid
    gridData={gridData}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With server side sorting

<Story name="With server side sorting">
    <GridComponent enableServersideSorting />;
</Story>

```jsx
// Pass 'serverSideSorting' function as a prop and Grid will call this function whenever the user selects the group sort option.
// Grid expect this function to re-render the Grid with sorted 'gridData'.
// In case if the developer is making an API call to get sorted data/sorting will take much time, use 'displayLoader' property to display loading graphics in the Grid, till you are ready with sorted data.
// Pass displayLoader={true}, then fetch sorted data and once you are ready with sorted data, update gridData and set displayLoader={false}

<Grid
    gridData={gridData}
    serverSideSorting={serverSideSorting}
    displayLoader={displayLoader}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With server side exporting

<Story name="With server side exporting">
    <GridComponent
        hasPagination
        passRowActions
        enableServersideExporting
        fileName="Custom filename"
        pdfPaperSize="A1"
    />
    ;
</Story>

```jsx
// Refer serverSideExporting in the 'Grid properties' section (top).
// Pass 'serverSideExporting' function as a prop and Grid will call this function whenever user tries to export data.

// In this storybook, server side exporting is implemented only for the 'Normal Grid'. It is not implemented for the 'Sub Component Grid'.
// You can follow the same implementation for both 'Normal Grid' and 'Sub Component Grid'.

<Grid
    gridData={gridData}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    serverSideExporting={serverSideExporting}
    columns={columns}
    idAttribute={idAttribute}
    rowActions={rowActions}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>
```

#### With react component as Grid Headers

<Story name="With react component as Grid Headers">
    <GridComponent enableJsxHeaders />;
</Story>

```jsx
// If required pass JSX element as Header of a column.
// But in that case, also make sure to pass a string value to another property 'title', which can be used for representing a column in components such as the group sort, manage columns, and export overlays.

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With grouped headers

<Story name="With grouped headers">
    <GridComponent enableGroupHeaders />;
</Story>

```jsx
// If a property 'groupHeader' is present in the column configuration. The Grid will combine all columns with the same 'groupHeader' value under a column heading.
// But, all column based functionalities such as sorting, filtering, etc. will depend on the 'Header' value and not the 'groupHeader' value.

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With custom overscan count

<Story name="With custom overscan count">
    <GridComponent rowsToOverscan={4} />;
</Story>

```jsx
// <Number specified in the rowsToOverscan prop> records will be over scanned by react-window.
// By default Grid takes 5 as rowsToOverscan.

<Grid
    gridData={gridData}
    rowsToOverscan={4}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
/>
```

#### With row specific expand icon and classNames

<Story name="With row specific expand icon and classNames">
    <GridComponent passGetRowInfo expandableColumn />;
</Story>

```jsx
// Refer getRowInfo in the 'Grid properties' section (top).

<Grid
    gridData={gridData}
    columns={columns}
    expandableColumn
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
/>
```

#### Hide grid header

<Story name="Hide grid header">
    <GridComponent gridHeader={false} passIdAttribute />;
</Story>

```jsx
// This should be a boolean value. Value 'False' will not render the Grid column header.
const gridHeader = false;

<Grid
    gridData={gridData}
    idAttribute={idAttribute}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    gridHeader={gridHeader}
/>;
```

#### Hide grid title

<Story name="Hide grid title">
    <GridComponent showTitle={false} passIdAttribute />;
</Story>

```jsx
// This should be a boolean value. Value 'False' will not render the Grid title.
const showTitle = false;

<Grid
    gridData={gridData}
    idAttribute={idAttribute}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    showTitle={showTitle}
/>;
```

#### Hide row selection

<Story name="Hide row selection">
    <GridComponent rowSelector={false} />;
</Story>

```jsx
// This should be a boolean value. Value 'False' will not render the row selector column.
const rowSelector = false;

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    rowSelector={rowSelector}
/>;
```

#### Hide globalSearch

<Story name="Hide globalSearch">
    <GridComponent globalSearch={false} />;
</Story>

```jsx
// This should be a boolean value. Value 'False' will not render the global search component.
const globalSearch = false;

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    globalSearch={globalSearch}
/>;
```

#### Hide columnFilter

<Story name="Hide columnFilter">
    <GridComponent columnFilter={false} />;
</Story>

```jsx
// This should be a boolean value. Value 'False' will not render the column filter component.
const columnFilter = false;

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    columnFilter={columnFilter}
/>;
```

#### Hide groupSort

<Story name="Hide groupSort">
    <GridComponent groupSort={false} />;
</Story>

```jsx
// This should be a boolean value. Value 'False' will not render the group sort component.
const groupSort = false;

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    groupSort={groupSort}
/>;
```

#### Hide columnChooser

<Story name="Hide columnChooser">
    <GridComponent columnChooser={false} />;
</Story>

```jsx
// This should be a boolean value. Value 'False' will not render the column chooser component.
const columnChooser = false;

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    columnChooser={columnChooser}
/>;
```

#### Hide exportData

<Story name="Hide exportData">
    <GridComponent exportData={false} />;
</Story>

```jsx
// This should be a boolean value. Value 'False' will not render the export data component.
const exportData = false;

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    exportData={exportData}
/>;
```

#### With custom file name for exported files

<Story name="With custom file name for exported files">
    <GridComponent fileName="Custom filename" />;
</Story>

```jsx
// This should be a string value. If prop is specified this will be the name of the exported files (with different extensions).
const fileName="Custom filename"

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    fileName={fileName}
/>;
```

#### With custom paper size for PDF export

<Story name="With custom paper size for PDF export">
    <GridComponent pdfPaperSize="A1" />;
</Story>

```jsx
// This should be a string value. If prop is specified this will be the paper size for PDF export.
// By default the paper size for PDF export will be A4
const pdfPaperSize = "A1"  // Use A1, A2, A3, A4 or A5 - Default value is A4

<Grid
    gridData={gridData}
    columns={columns}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    pdfPaperSize={pdfPaperSize}
/>;
```

#### With all props

<Story name="With all props">
    <GridComponent
        allProps
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
    />
    ;
</Story>

```jsx
<Grid
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>
```

#### With auto page refresh

<Story name="With auto page refresh">
    <GridComponent
        allProps
        previousPageRefresh
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
    />
    ;
</Story>

```jsx
// This will work only in the case of index based pagination.
// Follow this method if you are using a normal or sub component grid (not a tree view grid). For the tree view grid refer story "Tree view with auto refresh".
// While loading a new page (Say page 3) Grid will identify if the value of the property 'total' gets changed in pageInfo object.
// Once the value is changed, Grid considers all pages loaded before (Pages 1 and 2) as invalid pages.
// When the user is scrolling grid rows, if a row from an invalid page is being bound to the DOM, the Grid will trigger loadMoreData (with pageInfo containing that invalid pageNum).
// Once loadMoreData is called with a previous page, fetch data for that page again and replace the old page data in gridData with the new data.

// In this story, the value of the property 'total' will get updated after loading page 3.
// Once it happens, pages 2 and 1 should reload based on the scroll position.

<Grid
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
    previousPageRefresh
/>
```

#### Tree view with all props

<Story name="Tree view with all props">
    <GridComponent
        allProps
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
        treeStructure
    />
    ;
</Story>

```jsx
// Refer 'Tree Grid' region in the 'Grid properties' section (top).

<Grid
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    parentColumn={parentColumn}
    parentIdAttribute={parentIdAttribute}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>
```

#### Tree view with fixed row height

<Story name="Tree view with fixed row height">
    <GridComponent
        allProps
        className="icargoCustomClass"
        title="AWBs"
        fixedRowHeight
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
        treeStructure
    />
    ;
</Story>

```jsx
// All rows will have the same height (height of the first row).
// Only the height of the first row will be calculated and this height will be used for all other rows.
const fixedRowHeight = true;

<Grid
    className={className}
    title={title}
    fixedRowHeight={fixedRowHeight}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    parentColumn={parentColumn}
    parentIdAttribute={parentIdAttribute}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>;
```

#### Tree view without expansion

<Story name="Tree view without expansion">
    <GridComponent
        allProps
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
        treeStructure
        parentRowExpandable={false}
    />
    ;
</Story>

```jsx
// All parent rows will be open by default, and there won't be any 'expand/collapse icons' for parent rows.
const parentRowExpandable = false;

<Grid
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    parentColumn={parentColumn}
    parentIdAttribute={parentIdAttribute}
    parentRowExpandable={parentRowExpandable}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>;
```

#### Tree view with parent rows expanded

<Story name="Tree view with parent rows expanded">
    <GridComponent
        allProps
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
        treeStructure
        parentRowsToExpand={[0, 2]}
    />
    ;
</Story>

```jsx
// parentRowsToExpand accepts an array of parent idAttribute values. All parents whose idAttribute value is specified in parentRowsToExpand, will be in opened state by default.
// Still, expand/collapse icon for parent rows will be present, for user to expand and collapse parent rows.
const parentRowsToExpand = [0, 2];

<Grid
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    parentColumn={parentColumn}
    parentIdAttribute={parentIdAttribute}
    parentRowsToExpand={parentRowsToExpand}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>;
```

#### Tree view grid with server side exporting

<Story name="Tree view grid with server side exporting">
    <GridComponent
        allProps
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
        treeStructure
        enableServersideExporting
    />
    ;
</Story>

```jsx
// Refer serverSideExporting in the 'Grid properties' section (top).
// Pass 'serverSideExporting' function as a prop and Grid will call this function whenever user tries to export data.
// In this case of tree view grid, this function will be called only once. Grid expects complete data to be returned in that single call.
// return {
//     { data: <completeData> }
// }

// In this storybook, only few data is being returned for server side export of tree view Grid.

<Grid
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    parentColumn={parentColumn}
    parentIdAttribute={parentIdAttribute}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    serverSideExporting={serverSideExporting}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>
```

#### Tree view with auto refresh

<Story name="Tree view with auto refresh">
    <GridComponent
        allProps
        previousPageRefresh
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
        treeStructure
    />
    ;
</Story>

```jsx
// While loading a new page (Say page 3) under a parent, Grid will identify if the value of the property 'total' gets changed in the childData object.
// Once the value is changed, Grid considers all data loaded till now as invalid data, and immediately triggers loadMoreData callback.
// In loadMoreData, updatedPageInfo parameter will be null, parentId will be present and the third parameter isReload will be true.
// When this function is called, fetch all child data of that parentId and update it in the gridData.

// In this storybook, this functionality is implemented only for the first parent.
// Value of property 'total' will get updated while loading the 3rd page of the child data under the first parent.

<Grid
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    parentColumn={parentColumn}
    parentIdAttribute={parentIdAttribute}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>
```

#### Sub components with all props

<Story name="Sub components with all props">
    <GridComponent
        allProps
        isSubComponentGrid
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
    />
    ;
</Story>

```jsx
// Refer 'Sub Component Grid' in the 'Grid properties' section (top).

<Grid
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    subComponentIdAttribute={subComponentIdAttribute}
    subComponentColumns={subComponentColumns}
    subComponentColumnToExpand={subComponentColumnToExpand}
    subComponentHeader={subComponentHeader}
    onSubComponentRowSelect={onSubComponentRowSelect}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>
```

#### Sub components without headers

<Story name="Sub components without headers">
    <GridComponent
        allProps
        isSubComponentGrid
        subComponentHeader={false}
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
    />
    ;
</Story>

```jsx
// Column heading row for sub components will not be present.
const subComponentHeader = false;

<Grid
    subComponentHeader={false}
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={5}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    subComponentIdAttribute={subComponentIdAttribute}
    subComponentColumns={subComponentColumns}
    subComponentColumnToExpand={subComponentColumnToExpand}
    subComponentHeader={subComponentHeader}
    onSubComponentRowSelect={onSubComponentRowSelect}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
/>;
```

#### Sub components with auto page refresh

<Story name="Sub components with auto page refresh">
    <GridComponent
        allProps
        previousPageRefresh
        isSubComponentGrid
        className="icargoCustomClass"
        title="AWBs"
        gridWidth="100%"
        rowsToOverscan={4}
        CustomPanel={CustomPanel}
        fileName="Custom filename"
        rowsForSelection={[1, 7, 17]}
        pdfPaperSize="A1"
    />
    ;
</Story>

```jsx
// This will work only in the case of index based pagination.
// Follow this method if you are using a normal or sub component grid (not a tree view grid). For the tree view grid refer story "Tree view with auto refresh".
// While loading a new page (Say page 3) Grid will identify if the value of the property 'total' gets changed in the pageInfo object.
// Once the value is changed, Grid considers all pages loaded before (Pages 1 and 2) as invalid pages.
// When the user is scrolling grid rows, if the main row from an invalid page is being bound to the DOM, the Grid will trigger loadMoreData (with pageInfo containing that invalid pageNum).
// Once loadMoreData is called with a previous page, fetch main row data along with sub component data for each row, for that page again and replace the old page data in gridData with the new data.

// In this story, the value of the property 'total' will get updated after loading page 3.
// Once it happens, pages 2 and 1 should reload based on the scroll position.

<Grid
    className={className}
    title={title}
    gridWidth={gridWidth}
    gridData={gridData}
    rowsToOverscan={4}
    idAttribute={idAttribute}
    paginationType={paginationType}
    pageInfo={gridPageInfo}
    loadMoreData={loadMoreData}
    columns={columns}
    columnToExpand={columnToExpand}
    rowActions={rowActions}
    expandableColumn={expandableColumn}
    subComponentIdAttribute={subComponentIdAttribute}
    subComponentColumns={subComponentColumns}
    subComponentColumnToExpand={subComponentColumnToExpand}
    subComponentHeader={subComponentHeader}
    onSubComponentRowSelect={onSubComponentRowSelect}
    onRowUpdate={onRowUpdate}
    onRowSelect={onRowSelect}
    onSearch={onSearch}
    getRowInfo={getRowInfo}
    onGridRefresh={onGridRefresh}
    CustomPanel={CustomPanel}
    rowsToSelect={rowsToSelect}
    rowsToDeselect={rowsToDeselect}
    gridHeader
    rowSelector
    globalSearch
    columnFilter
    groupSort
    columnChooser
    exportData
    fileName={fileName}
    pdfPaperSize={pdfPaperSize}
    previousPageRefresh
/>
```
